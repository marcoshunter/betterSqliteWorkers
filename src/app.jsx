import * as React from 'react';
import {createRoot} from 'react-dom/client';

const root = createRoot(document.getElementById("root"));
root.render(
    <div>
        <p>Please toggle developer tools to see error or success message</p>
    </div>
);